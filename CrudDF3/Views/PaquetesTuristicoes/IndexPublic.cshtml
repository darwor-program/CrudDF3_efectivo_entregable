@model IEnumerable<CrudDF3.Models.PaquetesTuristico>

@{
    ViewData["Title"] = "Experiencias Exclusivas";
}

@section Styles {
    <style>
        :root {
            --primary-color: #1a3e3f;
            --secondary-color: #d4af37;
            --accent-color: #e8c07d;
            --light-bg: #f9f5f0;
            --dark-text: #2d2d2d;
            --light-text: #f8f9fa;
            --card-bg: #ffffff;
            --transition-speed: 0.4s;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            color: var(--dark-text);
            background-color: var(--light-bg);
            line-height: 1.6;
        }

        h1, h2, h3, h4 {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
        }

        /* Hero Section con Animación */
        .hero-section {
            min-height: 70vh;
            padding: 5rem 0;
            background: linear-gradient(rgba(26, 62, 63, 0.85), rgba(26, 62, 63, 0.85)), url('@Url.Content("~/Imagenes/travel-banner.jpg")') no-repeat center center/cover;
            color: white;
            display: flex;
            align-items: center;
            margin-bottom: 4rem;
            text-align: center;
            position: relative;
            overflow: hidden;
            animation: fadeIn 1.2s ease-out;
        }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            transform: translateY(0);
            transition: transform 0.6s ease;
        }

        .hero-section:hover .hero-content {
            transform: translateY(-10px);
        }

        .section-title {
            position: relative;
            display: inline-block;
            margin-bottom: 2.5rem;
        }

            .section-title::after {
                content: "";
                position: absolute;
                bottom: -12px;
                left: 50%;
                transform: translateX(-50%);
                width: 100px;
                height: 4px;
                background: linear-gradient(90deg, transparent, var(--secondary-color), transparent);
                animation: lineExpand 1.5s ease-out;
            }

        @@keyframes lineExpand {
            from

        {
            width: 0;
            opacity: 0;
        }

        to {
            width: 100px;
            opacity: 1;
        }

        }

        /* Tarjetas de Paquetes Mejoradas */
        .package-card {
            transition: all var(--transition-speed) cubic-bezier(0.25, 0.8, 0.25, 1);
            border-radius: 16px;
            overflow: hidden;
            border: none;
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.1);
            margin-bottom: 2.5rem;
            height: 100%;
            background-color: var(--card-bg);
            position: relative;
            transform-style: preserve-3d;
            perspective: 1000px;
        }

            .package-card:hover {
                transform: translateY(-15px) rotateX(5deg);
                box-shadow: 0 25px 60px rgba(0, 0, 0, 0.15);
            }

        .package-image-container {
            position: relative;
            overflow: hidden;
            height: 280px;
            clip-path: polygon(0 0, 100% 0, 100% 85%, 0 100%);
            transition: all var(--transition-speed) ease;
        }

        .package-card:hover .package-image-container {
            clip-path: polygon(0 0, 100% 0, 100% 90%, 0 100%);
        }

        .package-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.8s ease, filter 0.5s ease;
            filter: brightness(0.95);
        }

        .package-card:hover .package-image {
            transform: scale(1.1) rotate(1deg);
            filter: brightness(1);
        }

        .package-badge {
            position: absolute;
            top: 25px;
            right: 25px;
            background-color: var(--secondary-color);
            color: var(--primary-color);
            padding: 0.6rem 1.5rem;
            border-radius: 30px;
            font-weight: 800;
            font-size: 1rem;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
            z-index: 3;
            transform: translateY(0) rotate(0);
            transition: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        .package-card:hover .package-badge {
            transform: translateY(-5px) rotate(5deg);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }

        .package-card-body {
            padding: 2rem;
            position: relative;
        }

        .package-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            margin-bottom: 1.2rem;
            color: var(--primary-color);
            position: relative;
            display: inline-block;
        }

            .package-title::after {
                content: "";
                position: absolute;
                bottom: -10px;
                left: 0;
                width: 60px;
                height: 3px;
                background: linear-gradient(90deg, var(--secondary-color), transparent);
                transition: width 0.5s ease;
            }

        .package-card:hover .package-title::after {
            width: 100px;
        }

        .package-description {
            color: #666;
            margin-bottom: 1.8rem;
            font-size: 1rem;
            line-height: 1.8;
            transition: all 0.3s ease;
        }

        .package-card:hover .package-description {
            color: #555;
        }

        .destination-info {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
            color: var(--primary-color);
            font-size: 1.1rem;
        }

            .destination-info i {
                margin-right: 12px;
                color: var(--secondary-color);
                font-size: 1.4rem;
                transition: transform 0.3s ease;
            }

        .package-card:hover .destination-info i {
            transform: scale(1.2);
        }

        /* Acordeones Modernos */
        .accordion-section {
            margin: 1.8rem 0;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 3px 15px rgba(0, 0, 0, 0.05);
        }

        .accordion-header {
            background-color: var(--light-bg);
            padding: 1rem 1.5rem;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;
        }

            .accordion-header:hover {
                background-color: #f0e9e1;
            }

            .accordion-header h5 {
                margin: 0;
                font-weight: 600;
                color: var(--primary-color);
                display: flex;
                align-items: center;
            }

                .accordion-header h5 i {
                    margin-right: 10px;
                    color: var(--secondary-color);
                    transition: transform 0.3s ease;
                }

            .accordion-header.active h5 i {
                transform: rotate(90deg);
            }

        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease, padding 0.3s ease;
            background-color: white;
        }

        .accordion-header.active + .accordion-content {
            max-height: 500px;
            padding: 1rem 1.5rem;
        }

        .feature-list {
            list-style: none;
            padding-left: 0;
            margin: 0;
        }

            .feature-list li {
                padding: 0.7rem 0;
                border-bottom: 1px dashed #eee;
                display: flex;
                align-items: center;
                transition: all 0.3s ease;
            }

                .feature-list li:hover {
                    transform: translateX(5px);
                    border-bottom-color: var(--accent-color);
                }

                .feature-list li:last-child {
                    border-bottom: none;
                }

                .feature-list li i {
                    margin-right: 10px;
                    color: var(--secondary-color);
                    font-size: 0.9rem;
                    min-width: 20px;
                }

        /* Footer de Tarjeta */
        .package-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 1.5rem;
            margin-top: 1.5rem;
            border-top: 1px solid #f0f0f0;
        }

        .travel-info {
            display: flex;
            align-items: center;
            color: #777;
            font-size: 0.95rem;
            transition: all 0.3s ease;
        }

        .package-card:hover .travel-info {
            color: #666;
        }

        .travel-info i {
            margin-right: 8px;
            color: var(--secondary-color);
            font-size: 1.1rem;
        }

        /* Efectos Especiales */
        .package-card::after {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.05) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.6s ease;
            pointer-events: none;
            z-index: 1;
        }

        .package-card:hover::after {
            opacity: 1;
        }

        /* Responsive */
        @@media (max-width: 1200px) {
            .package-image-container {
                height: 240px;
            }
        }

        @@media (max-width: 992px) {
            .package-card {
                margin-bottom: 2rem;
            }

            .package-image-container {
                height: 220px;
            }

            .package-title {
                font-size: 1.6rem;
            }
        }

        @@media (max-width: 768px) {
            .hero-section {
                min-height: 60vh;
                padding: 4rem 0;
            }

            .section-title {
                font-size: 2.2rem;
            }
        }

        @@media (max-width: 576px) {
            .hero-section {
                min-height: 50vh;
            }

            .section-title {
                font-size: 1.9rem;
            }

            .package-image-container {
                height: 200px;
                clip-path: polygon(0 0, 100% 0, 100% 90%, 0 100%);
            }

            .package-card-body {
                padding: 1.5rem;
            }
        }
    </style>
}

<!-- Hero Section Animada -->
<section class="hero-section">
    <div class="hero-overlay"></div>
    <div class="container hero-content">
        <h1 class="display-4 fw-bold mb-4 animate__animated animate__fadeInDown">@ViewData["Title"]</h1>
        <p class="lead mb-4 animate__animated animate__fadeIn animate__delay-1s">Descubre experiencias únicas diseñadas para crear memorias inolvidables</p>
        <div class="section-title animate__animated animate__fadeIn animate__delay-2s"></div>
    </div>
</section>

<!-- Paquetes Section con Efectos -->
<div class="container">
    <div class="row">
        @{
            var packageImages = new List<string> {
        "/Imagenes/Paquete1.jpg",
        "/Imagenes/Paquete2.jpg",
        "/Imagenes/Paquete3.jpg",
        "/Imagenes/Paquete4.jpg",
        "/Imagenes/Paquete5.jpg",
        "/Imagenes/Paquete6.jpg"
        };

            int imageIndex = 0;
        }

        @foreach (var item in Model)
        {
            <div class="col-lg-4 col-md-6 mb-5">
                <div class="package-card">
                    <div class="package-image-container">
                        <img src="@packageImages[imageIndex % packageImages.Count]" class="package-image" alt="@item.NombrePaquete">
                        <div class="package-badge">
                            @item.PrecioPaquete?.ToString("C")
                        </div>
                    </div>

                    <div class="package-card-body">
                        <h3 class="package-title">@item.NombrePaquete</h3>
                        <p class="package-description">@item.DescripcionPaquete</p>

                        <div class="destination-info">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>@item.DestinoPaquete</span>
                        </div>

                        <!-- Acordeón para Habitaciones -->
                        <div class="accordion-section">
                            <div class="accordion-header">
                                <h5><i class="fas fa-bed"></i> Habitaciones Incluidas</h5>
                                <i class="fas fa-chevron-right"></i>
                            </div>
                            <div class="accordion-content">
                                <ul class="feature-list">
                                    @foreach (var habitacion in item.PaqueteHabitaciones)
                                    {
                                        <li>
                                            <i class="fas fa-door-open"></i>
                                            @habitacion.IdHabitacionNavigation?.TipoHabitacion
                                        </li>
                                    }
                                </ul>
                            </div>
                        </div>

                        <!-- Acordeón para Servicios -->
                        <div class="accordion-section">
                            <div class="accordion-header">
                                <h5><i class="fas fa-concierge-bell"></i> Servicios Incluidos</h5>
                                <i class="fas fa-chevron-right"></i>
                            </div>
                            <div class="accordion-content">
                                <ul class="feature-list">
                                    @foreach (var servicio in item.PaqueteServicios)
                                    {
                                        <li>
                                            <i class="fas fa-check-circle"></i>
                                            @servicio.IdServicioNavigation?.NombreServicio
                                        </li>
                                    }
                                </ul>
                            </div>
                        </div>

                        <div class="package-footer">
                            <div class="travel-info">
                                <i class="fas fa-calendar-alt"></i>
                                <span>@item.FechaPaquete?.ToString("d")</span>
                            </div>
                            <div class="travel-info">
                                <i class="fas fa-clock"></i>
                                <span>@item.TipoViajePaquete</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            imageIndex++;
        }
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Acordeones interactivos
            const accordionHeaders = document.querySelectorAll('.accordion-header');

            accordionHeaders.forEach(header => {
                header.addEventListener('click', function() {
                    this.classList.toggle('active');
                    const content = this.nextElementSibling;

                    if (this.classList.contains('active')) {
                        content.style.maxHeight = content.scrollHeight + 'px';
                    } else {
                        content.style.maxHeight = '0';
                    }

                    // Rotar ícono
                    const icon = this.querySelector('.fa-chevron-right');
                    icon.style.transform = this.classList.contains('active') ? 'rotate(90deg)' : 'rotate(0)';
                });
            });

            // Efecto de aparición gradual para las tarjetas
            const packageCards = document.querySelectorAll('.package-card');

            packageCards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = `all 0.6s ease ${index * 0.1}s`;

                setTimeout(() => {
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, 100);
            });
        });
    </script>
}